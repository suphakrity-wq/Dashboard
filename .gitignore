.DS_Store
*.log

# เอกสารงานวิจัย (PDF ลิขสิทธิ์ของผู้อื่น) — ระบบไม่ได้อ่านตอนรัน
# ตัวเลขที่ใช้ถูกคัดมาไว้ใน assets/js/core/benchmarks.js พร้อมเลขหน้าแล้ว
/เอกสาร/

# ฟอนต์ต้นฉบับ .ttf ที่โฟลเดอร์ราก — เว็บใช้ .woff2 ใน assets/fonts/ แทน
# (ต้องมี / นำหน้า ไม่งั้นจะไปตรงกับ assets/fonts/ ด้วย แล้วเว็บจะไม่มีฟอนต์)
/fonts/
