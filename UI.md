# ระบบ UI ของ Dashboard นี้

ไฟล์นี้คือ "กติกากลาง" ของหน้าตาทั้งเว็บ
ถ้าจะเพิ่มหรือแก้อะไร ให้หยิบค่าจากที่นี่ อย่าใส่ค่าดิบ (px / hex) ลงในคอมโพเนนต์

ค่าทั้งหมดอยู่ที่ [`assets/css/parts/1-tokens.css`](assets/css/parts/1-tokens.css) ไฟล์เดียว
แก้ที่นั่นแล้วมีผลทั้งเว็บพร้อมกัน

---

## 1. สี

### สีพื้นผิว
| โทเคน | ค่า | ใช้กับ |
|---|---|---|
| `--canvas` | `#f5f5f7` | พื้นหลังหน้าเว็บ |
| `--surface` | `#fff` | พื้นการ์ด / แถบเมนู |
| `--surface-2` | `#fafafc` | กล่องซ้อนในการ์ด |
| `--line` | `#e6e6ea` | เส้นขอบ |
| `--hair` | `#f0f0f4` | พื้นตอนชี้เมาส์ |

### สีข้อมูล — สีบอก "สถานะ" ไม่ใช่ชื่อชุดข้อมูล
| โทเคน | ค่า | ความหมาย |
|---|---|---|
| `--purple` | `#6d4dfb` | **ฝั่งที่มีค่ามากกว่า** / ตัวเอกของงานวิจัย |
| `--graphite` | `#2a2a30` | อีกฝั่ง / ค่าอ้างอิง |
| `--track` | `#e4e1ee` | ช่องที่ยังไม่ถึงค่า (ตอนปกติ) |
| `--track-hover` | `#c9c2de` | ช่องที่ยังไม่ถึงค่า (ตอนชี้แถว) |
| `--lime` | `#c9f24e` | ทางแก้ / สิ่งที่ดี — ใช้เป็นพื้น ตัวอักษรใช้ `--lime-ink` |
| `--red` | `#e0483d` | ตัวเลขที่เป็นปัญหาเท่านั้น |

> **กฎที่ห้ามลืม:** `--track-hover` ต้องเข้มกว่า `--hair` อย่างน้อย ~40 ระดับความสว่าง
> ไม่งั้นพอชี้แถว ช่องว่างจะกลืนไปกับพื้นจนไม่เห็นว่าหลอดจบตรงไหน (เคยเป็นบั๊กมาแล้ว 2 รอบ)

### สีบนพื้นเข้ม (การ์ดคำตอบ)
`--on-dark` `--on-dark-muted` `--lime-on-dark` `--purple-on-dark` `--gray-on-dark`
ชุดปกติสว่างไม่พอเมื่ออยู่บนพื้นดำ จึงมีชุดแยก

---

## 2. ตัวอักษร

ฟอนต์ **DB Heavent** เล็กกว่าฟอนต์ทั่วไปราว 20% ขนาดฐานจึงตั้งสูงกว่าปกติ

| โทเคน | จอคอม | ใช้กับ |
|---|---|---|
| `--t-xs` | 19px | ป้ายกำกับ, คำอธิบายย่อย |
| `--t-sm` | 21px | ข้อความทั่วไป |
| `--t-md` | 23px | หัวข้อย่อย |
| `--t-lg` | 27px | หัวข้อการ์ด |
| `--t-xl` | 33px | ชื่อหน้า |
| `--t-2xl` | 42px | ตัวเลขเด่น |
| `--t-3xl` | 60px | ตัวเลขใหญ่สุด |

จอเล็กไม่ได้แก้ขนาดรายคอมโพเนนต์ แต่**ลดค่าโทเคนทั้งชุด** ใน media query
(ดูหัวข้อ 6 ของ `3-layout.css`) สัดส่วนทั้งหน้าจึงย่อลงพร้อมกันอย่างสม่ำเสมอ

ขนาดไอคอน: `--ico-sm 18` `--ico-md 19` `--ico-lg 22` `--ico-xl 24`

---

## 3. ระยะห่างและความมน

**ระยะ:** `--sp-1 8` `--sp-2 14` `--sp-3 20` `--sp-4 28` `--sp-5 40`

**ความมนของมุม — ใช้ 6 ขั้นนี้เท่านั้น**
| โทเคน | ค่า | ใช้กับ |
|---|---|---|
| `--r-xs` | 4px | บล็อกเล็กในหลอดวัดค่า |
| `--r-sm` | 9px | ปุ่ม, ไอคอน, ป้าย |
| `--r-md` | 12px | แถวข้อมูล, ช่องกรอก |
| `--r-lg` | 16px | การ์ด (`--radius` ชี้มาที่นี่) |
| `--r-xl` | 20px | กล่องใหญ่พิเศษ เช่น การ์ดคำตอบ |
| `--r-pill` | 100px | ทรงแคปซูล |

**เงา 3 ระดับ:** `--sh-1` การ์ดทั่วไป · `--sh-2` การ์ดที่ต้องเด่นขึ้น · `--sh-3` การ์ดคำตอบ

---

## 4. การเคลื่อนไหว

| โทเคน | ค่า | ใช้กับ |
|---|---|---|
| `--ease` | `cubic-bezier(.22,1,.36,1)` | ทุกการเคลื่อนไหว — ออกตัวเร็ว ชะลอปลาย |
| `--t-fast` | .2s | สี, ไอคอน |
| `--t-base` | .32s | hover ของแถวและการ์ด |
| `--t-slow` | .5s | หลอดวิ่ง, แผงกางออก |

ผู้ใช้ที่ตั้งค่า *ลดการเคลื่อนไหว* ในระบบปฏิบัติการ จะถูกตัดอนิเมชันทั้งหมดให้อัตโนมัติ

---

## 5. แถวข้อมูล + hover — ประกาศที่เดียวทั้งเว็บ

ทุกแถวที่ผู้ใช้กวาดสายตาอ่าน (อันดับ, เทียบกลุ่ม, เทียบเหตุผล, คำอธิบายโดนัท,
วาฟเฟิล, แถบสัดส่วน, การ์ดสองฝั่ง) ใช้กฎ **ชุดเดียวกัน** ใน
[`4-components.css` หัวข้อ 4](assets/css/parts/4-components.css)

- กล่องมีกรอบ 1px `--line` มุมมน `--r-md`
- ชี้แล้ว: พื้นเป็น `--hair` · ขอบเป็น `--purple-3` · ยกตัวขึ้น 1px · มีเงา

> **ห้ามเขียน `:hover` แยกรายคอมโพเนนต์** ถ้ามีแถวชนิดใหม่ ให้เอา selector
> ไปต่อในลิสต์กลาง ไม่งั้น hover จะไม่เหมือนกันทั้งเว็บ (เคยเป็นปัญหามาแล้ว)

---

## 6. หลอดวัดค่า

ตัดเป็นบล็อกสี่เหลี่ยมมุมมนเรียงต่อกัน **1 บล็อก = 1 หน่วย** (1 ข่าว / 1 คน)
ตรรกะอยู่ที่ `segmentBars()` ใน [`assets/js/ui/dom.js`](assets/js/ui/dom.js)

- **จอเล็ก = ลดจำนวนบล็อก ไม่ใช่เลิกแบ่ง** รวบเป็น 1 บล็อก = 2, 3, … หน่วย
  ค่ายังตรงตามจริง เพราะบล็อกสุดท้ายเติมตามเศษ (ชี้ค้างมีข้อความบอกว่า 1 ช่องเท่ากับเท่าไร)
- ตัดสินใจ **ทั้งกราฟพร้อมกัน** ไม่ใช่ทีละหลอด ไม่งั้นหน้าตาจะปนกัน
- **วาดใหม่เมื่อย่อ/ขยายหน้าต่าง** ไม่งั้นจำนวนบล็อกค้างอยู่กับความกว้างตอนโหลด
ใช้กับหลอดทุกชนิดในเว็บ: อันดับข่าว · เทียบตามกลุ่ม · เทียบเหตุผล · การ์ดสองฝั่ง ·
แถบในการ์ดคำตอบ · การ์ดสาเหตุ · การ์ดธีมจากคำตอบปลายเปิด
(ยกเว้นแถบสัดส่วน 100% กับกราฟที่ไม่ใช่หลอด เช่น โดนัท วาฟเฟิล ฮีตแมป)

- ปรับความถี่ของช่องได้ 3 ค่าใน `dom.js`
  `MAX_BLOCKS` (3) เพดานจำนวนช่อง · `MIN_BLOCK_PX` (46) ความกว้างขั้นต่ำต่อช่อง ·
  `MIN_BLOCKS` (3) พื้นจำนวนช่อง
  อยากได้ช่องยาวขึ้น = ลด `MAX_BLOCKS` หรือเพิ่ม `MIN_BLOCK_PX`

- **ช่องไฟระหว่างช่องต้องกว้างพอให้เห็นเป็นคนละช่อง** ตอนนี้ 7px (ช่องแคบ) และ 10px (ช่องยาว)
  ค่าเดิม 4/6px บนหลอดสูง 18–26px ตาอ่านเป็น *เส้นแบ่ง* ไม่ใช่ *ช่องว่าง*
  หลอดจึงดูเป็นแท่งเดียวที่มีรอยต่อ แทนที่จะเป็นช่องให้นับ
  หลักเดียวกับช่องว่างระหว่างชิ้นของวงกลมสัดส่วน
- **ช่องว่างไม่ได้แทนค่าใด** ต้องบอกไว้ในบรรทัดสเกลเสมอ ไม่งั้นผู้อ่านจะนับช่องว่างเป็นค่าที่หายไป
- อนิเมชันอยู่ที่ **หลอดทั้งอัน** (`segSweep` บน `.is-seg`) ไม่ใช่ทีละช่อง
  ถ้าย้ายไปไว้ที่ `.seg-blk` หลอดจะเด้งขึ้นเป็นช่อง ๆ ซึ่งเคยถูกตีกลับมาแล้ว

---

## 7. จุดตัดของขนาดจอ

| ความกว้าง | เปลี่ยนอะไร |
|---|---|
| ≤ 1180px | แถบเมนูซ้ายแคบลง |
| ≤ 1000px | แถบเมนูย้ายขึ้นด้านบน |
| ≤ 920px | แท็บเหลือไอคอน (พร้อมช่วง 1001–1180 ที่แคบพอกัน) |
| ≤ 760px | **มือถือ** — เมนูยุบเป็นปุ่ม, เครื่องมือขึ้นไปบนสุด |
| ≤ 620px | ลดขนาดตัวอักษรและระยะทั้งชุด |
| ≤ 400px | โทรศัพท์เครื่องเล็ก |
| ≤ 330px | แคบสุดขั้ว |

**กฎของแถวเครื่องมือ (ตัวกรอง + แท็บ):** จอตั้งแต่ 761px ขึ้นไปต้องอยู่แถวเดียวกันเสมอ
ถ้าที่ไม่พอให้ "หด" ไม่ใช่ตกบรรทัด โดยช่องที่ยาวกว่าหดก่อน

---

## 8. เช็คลิสต์ก่อน commit งาน UI

1. ไม่มีค่าดิบ px/hex ในคอมโพเนนต์ — ใช้โทเคนหมด
2. เพิ่มแถวชนิดใหม่ → เอา selector ไปต่อในลิสต์ hover กลาง
3. ทดสอบอย่างน้อย 4 ความกว้าง: 1440 / 1024 / 768 / 375
4. ทดสอบ **ย่อ-ขยายหน้าต่างไปมา** ไม่ใช่แค่โหลดที่ขนาดเดียว
5. ไม่มีอะไรล้นจอ ไม่มีตัวหนังสือทับกัน ทุกช่องห่างขอบการ์ดอย่างน้อย 6px
6. **ตรวจว่ามองเห็นจริง ไม่ใช่แค่วางถูกที่** — เช็ค `opacity` / `visibility` ด้วย
   เพราะการวัดตำแหน่ง (`getBoundingClientRect`) ยังได้ค่าปกติแม้ของจะโปร่งใสจนหายไป
   (เคยพลาด: หลอดหายทั้งแถบหลังย่อจอ แต่สคริปต์ตรวจไม่จับ เพราะขนาดยังถูกต้อง)

> **กฎอนิเมชัน — ห้ามให้ "การมองเห็น" ขึ้นกับอนิเมชัน**
> ตั้ง `opacity:1` เป็นฐานเสมอ ใช้อนิเมชันเป็นของแถมเท่านั้น
> ห้ามตั้ง `opacity:0` แล้วพึ่ง animation พาไป 1 และห้ามใช้ `animation-fill-mode`
> กับสถานะที่มองไม่เห็น เพราะพังได้ 2 ทาง:
> 1. ตอนวาดใหม่แล้วปิดอนิเมชัน (`animation:none`) ของจะค้างที่มองไม่เห็น
> 2. ถ้าหน้าถูกเรนเดอร์ตอนแท็บซ่อนอยู่ เบราว์เซอร์ยังไม่เริ่มอนิเมชัน
>    ของจะค้างมองไม่เห็นจนกว่าจะกลับมาดูแท็บนั้น

## กราฟ — กฎการอ่าน (เพิ่ม 9/2569)

ทุกการ์ดกราฟต้องมีครบ 5 อย่าง อ่านการ์ดใบเดียวแล้วเข้าใจได้เลย
1. **ชื่อกราฟ** บอกว่ากำลังดูอะไร
2. **หน่วยติดอยู่กับตัวเลข** เช่น `20 คน · 80%` ไม่ใช่ `20` ลอย ๆ
3. **สเกล** — หลอดแบบแบ่งช่องมีบรรทัด `หลอดเต็ม = N · 1 ช่อง = N`
4. **บรรทัด "อ่านยังไง"** บอกวิธีตีความ (คลาส `.hint`)
5. **ฐานข้อมูล** `ฐานข้อมูล: ผู้ตอบ N คน` เติมให้อัตโนมัติในบล็อก charts (ปิดด้วย `base:false`)

ห้ามใช้:
- **วงกลมขนาดตามค่า** เทียบด้วยตาไม่แม่น ใช้หลอดเรียงอันดับแทน
- **จำนวนคนดิบข้ามกลุ่มที่ขนาดไม่เท่ากัน** ต้องแปลงเป็น % ของกลุ่มก่อน (ดู heatmap)
- **แท่งสองสีวางซ้อนกันในแกนเดียว** ตาต้องกระโดดไปมา ใช้แท่งสองฝั่ง (pyramid) แทน
- **เส้นหลายสิบเส้นตัดกัน (spaghetti)** ใช้ไอคอนคน (people) นับหัวแทน
- **กราฟที่มีสิบกว่าแถว/แกนสองฝั่ง** สำหรับคำถามง่าย ๆ ให้ยุบเป็นช่วงกว้าง 3 ช่วง
  คำถามว่า "คนส่วนใหญ่อยู่ตรงไหน" ไม่ต้องใช้ความละเอียดระดับทีละค่า
- **แถบซ้อน (stacked) เวลาต้องเทียบสองชุด** ชิ้นที่จะเทียบอยู่คนละตำแหน่ง ตาคนวัดไม่ได้
  ใช้แท่งเทียบ (grouped) ที่ทุกแท่งเริ่มจากเส้นซ้ายเดียวกันแทน
- **เปอร์เซ็นต์จากกลุ่มที่มีคนน้อยกว่า 5 คน** 1 ใน 2 คน = 50% ดูหนักแน่นพอ ๆ กับ 20 ใน 40 คน
  ต้องตัดกลุ่มเล็กออกแล้วบอกไว้ว่าตัดอะไรไปเพราะอะไร (ดู minGroup ใน heatmap)
- **โดนัทปลายมน** บนวงที่ต่อกันสนิททำไม่ได้ — หัวมนสองส่วนจะชนกันเป็นรอยเว้า
  ใช้ปลายตัดตรง + เส้นคั่นสีพื้นการ์ดแทน ได้ทั้งวงเต็มและค่าตรงเป๊ะ

## วงกลมสัดส่วน (pie) — ใช้เมื่อใด

ใช้ได้เฉพาะคำถามที่ **เลือกได้คำตอบเดียว** เท่านั้น เพราะทั้งวงต้องเท่ากับผู้ตอบทั้งหมดพอดี
คำถามที่เลือกได้หลายข้อ (เช่น ช่องทางรับข่าว) ผลรวมจะเกิน 100% แล้ววงกลมจะสื่อความผิด
ให้ใช้หลอดเรียงอันดับ (rank) แทน

**วาดแต่ละชิ้นเป็นรูปปิดที่ระบายสี ไม่ใช่เส้นหนา (`stroke`)**
เส้นหนาเลือกปลายได้แค่ตัดตรงหรือมนเต็มใบ จะเอากึ่งโค้งไม่ได้
และปลายมนยื่นเลยจุดปลายเส้นข้างละครึ่งความหนา จนกินช่องว่างระหว่างชิ้นหายไป
รูปปิดกำหนดรัศมีมุมได้เอง (`CORNER = 4` เทียบกับปลายมนเต็มใบที่เท่ากับ `W/2 = 11`)
และช่องว่างที่เว้นไว้เป็นช่องว่างจริงตามมุมที่คำนวณ ชิ้นเล็กจึงไม่บวมเกินค่าจริง

**ต้องมีรางสีเทาอยู่ชั้นหลัง แบ่งช่องด้วยมุมชุดเดียวกับชิ้นสี**
เพื่อให้เห็นว่าวงถูกแบ่งเป็นกี่ช่องแม้ชิ้นนั้นจะเล็กมาก
และเพื่อให้ช่องว่างอ่านเป็น "เส้นแบ่ง" ไม่ใช่ "ข้อมูลขาดหาย"
รางเว้นช่องแคบกว่าชิ้นสี (`TRACK_GAP = GAP * 0.4`) จึงโผล่พ้นชิ้นสีทั้งสองด้าน

ข้อกำหนดอื่น
1. หลอดของวงต้องหนาพอให้กล่องตัวเลขวางอยู่ในเนื้อหลอดได้เต็มใบ (`W = 22`, กล่องสูง 12)
2. เขียน `%` ลงบนชิ้นที่กินมุมตั้งแต่ 40 องศาขึ้นไป ชิ้นที่แคบกว่านั้นกล่องจะทับชิ้นข้างเคียง
3. **ตัวเลขบนกราฟต้องอยู่ในกล่องพื้นขาวมุมมน (`.pie-tagbox`) ห้ามใช้เส้นขอบตัวอักษร**
   (`stroke` / `paint-order` บนตัวอักษร) เส้นขอบกัดเนื้อตัวเลขให้บางลง พอไปทับสีอ่อนจะอ่านไม่ออก
   กฎนี้ใช้กับตัวเลขทุกตัวที่วางทับกราฟ ไม่ใช่แค่ pie
4. วาดกล่องตัวเลขทั้งชุดหลังวาดชิ้นครบแล้ว ไม่อย่างนั้นชิ้นถัดไปจะทับกล่องของชิ้นก่อนหน้า
5. ต้องมีคำอธิบายข้างวงเสมอ พร้อม `%` และจำนวนคนจริง ผู้อ่านจึงไม่ต้องเทียบสีจากความจำ
6. ชิ้นที่มีค่าเป็นศูนย์ต้องไม่ถูกวาด

**สีของ pie ใช้ `--pie-1..5` เท่านั้น ห้ามใช้ `--sc-1..5`**
ชุด `--sc-*` เป็นเฉดม่วงไล่ระดับ สองเฉดอ่อนสุดอยู่ใกล้กันเกินไป
ชิ้นเล็กสองชิ้นที่วางติดกันจะดูเป็นชิ้นเดียว ชุด `--pie-*` สลับเนื้อสีทีละชิ้น (ม่วง มะนาว ดำ ม่วงอ่อน เทาอ่อน)

จำนวนชิ้นที่เหมาะสมคือ 2–5 ชิ้น มากกว่านี้ชิ้นเล็กจะอ่านไม่ออก ให้ยุบเป็นกลุ่มกว้างก่อน

## หนึ่งส่วนประกอบ = หนึ่งน้ำหนักฟอนต์

**ในกล่องเดียว แถวเดียว หรือชิปเดียว ห้ามใช้น้ำหนักฟอนต์เกินหนึ่งค่า**
เคยมีชิปที่ใส่ครบสามน้ำหนัก (ชื่อ 400 / จำนวนคน 700 / % 400 จาง) อ่านแล้วสะดุดทุกใบ

ลำดับความสำคัญภายในส่วนประกอบเดียวกันบอกด้วย **สี** และ **ขนาด** เท่านั้น

    ตัวหลัก   color: var(--ink)
    ตัวรอง    color: var(--ink-2)
    ตัวประกอบ color: var(--muted)  + font-size เล็กลงหนึ่งขั้น

วิธีเขียน: ตั้ง `font-weight` ที่ตัวส่วนประกอบชั้นนอกครั้งเดียว
แล้วให้ลูกทุกตัวใช้ `font-weight:inherit` — ไม่ใช่ปล่อยให้ `<b>` เอา 700 ของเบราว์เซอร์มาเอง

ที่บังคับกฎนี้อยู่: `.cloud-chip` · `.pl-row` · `.dl-row` · `.rank-val` · `.heat-col` · `.stat-value`
(หัวข้อกับเนื้อหาที่อยู่คนละส่วนยังต่างน้ำหนักกันได้ตามปกติ กฎนี้พูดถึงของที่อยู่ในกล่องเดียวกัน)

## เขียนให้น้อย แต่ให้ได้ใจความ

หนึ่งการ์ดกราฟมีได้ไม่เกิน **สองบรรทัดข้อความ** นอกเหนือจากชื่อกราฟ

    ชื่อกราฟ
    note  — ข้อจำกัดของข้อมูลที่ต้องรู้ก่อนอ่าน (เช่น "เลือกได้หลายข้อ")
    [กราฟ]
    hint  — สิ่งที่อ่านออกมาได้ ซึ่งดูจากกราฟเฉย ๆ แล้วไม่รู้

กฎการตัด
- **hint ที่พูดซ้ำสิ่งที่กราฟบอกอยู่แล้ว ให้ลบทิ้ง**
  "แท่งที่ยาวกว่าคือฝั่งที่มากกว่า" ไม่ได้เพิ่มอะไร · "สองแท่งใช้สเกลเดียวกัน" เพิ่ม
- **บรรทัดฐานข้อมูลขึ้นเฉพาะตอนที่ยังไม่มีบรรทัดอื่นบอกจำนวนผู้ตอบ** ไม่ใช่ขึ้นทุกใบ
- หัวหน้ามีบรรทัดเดียว (`answers`) ส่วน `desc` ในไฟล์หน้าเป็นบันทึกของคนทำ ไม่ต้องขึ้นจอ
- ตัดคำที่ไม่เปลี่ยนความหมายเมื่อลบออก: "ทั้งวงจึงเท่ากับผู้ตอบทั้งหมด" ในเมื่อบรรทัดสเกลบอกอยู่แล้ว

## ลำดับของเมนู = เส้นเรื่องของรายงาน

เมนูด้านซ้ายไม่ได้เรียงตามความสำคัญ แต่เรียงตามลำดับที่ผู้อ่านควรอ่าน

    ภาพรวมการสำรวจ -> ผลการสำรวจ -> เหตุผลของคำตอบ
      -> ทางเลือกของผู้ตอบ -> สรุปผลและข้อเสนอแนะ
    (ปิดท้ายด้วยสไลด์นำเสนอ และข้อมูลดิบ ซึ่งเป็นส่วนประกอบ ไม่ใช่ขั้นของเรื่อง)

กฎที่ต้องรักษาไว้
- **ห้ามมีสองเมนูที่ชื่อใกล้กัน** เคยมี "สรุปผล" กับ "ข้อสรุป & ข้อเสนอ" อยู่พร้อมกัน
  ผู้อ่านแยกไม่ออกว่าต่างกันตรงไหน หน้าแรกจึงเปลี่ยนเป็น "ภาพรวมการสำรวจ"
- **คำว่า "สรุปผล" สงวนไว้ให้หน้าสุดท้ายเท่านั้น** บทสรุปต้องอยู่ท้ายเรื่อง ไม่ใช่ต้นเรื่อง

## สำนวนที่ใช้ในหน้าจอ

ทุกข้อความที่ผู้ใช้เห็นเป็นสำนวนรายงานผล ไม่ใช่สำนวนพูด

| ห้ามใช้ | ใช้แทน |
|---|---|
| เจอข่าวยังไง | ผู้ตอบเข้าถึงข่าวด้วยวิธีใด |
| ต่างกันแค่ไหน | ต่างกันเท่าใด |
| คนเลือกเยอะ | มีผู้เลือกมากที่สุด |
| ข่าวดราม่าชนะ | ข่าวดราม่าได้เปรียบ |
| ข้อบนสุดคุ้มที่สุด | ข้อแรกครอบคลุมผู้ตอบมากที่สุด |
| ยิ่งห่างยิ่งน่าห่วง | (ตัดออก อย่าตีความแทนผู้อ่าน) |

- **ห้ามใช้ขีดยาว (—) ในข้อความที่ผู้ใช้เห็น** ใช้จุดกลาง `·` คั่น หรือแยกเป็นประโยคใหม่
  (ในคอมเมนต์ของโค้ดใช้ได้ตามสะดวก)
- **ห้ามตีความแทนผู้อ่าน** บรรทัด `.hint` บอกได้แค่ *วิธีอ่าน* กราฟ
  ไม่ใช่บอกว่าผลลัพธ์ดีหรือแย่
- **เรียกผู้ตอบว่า "ผู้ตอบ" เสมอ** ไม่ใช่ "คน" หรือ "เรา"
- ข้อความในคอลัมน์ที่ตรงกับหัวข้อในชีต (`pages/columns.js`) **ห้ามแก้** ต้องตรงกับชีตทุกตัวอักษร

## กล่องเล็กที่ใส่ตัวเลข/ไอคอน

`display:grid; place-items:center` ยังไม่พอ — ฟอนต์ไทยมีส่วนบน/ล่างของบรรทัดไม่เท่ากัน
ตัวเลขจะเบ้ลงล่างราว 1–2px ต้องใส่ `line-height:1` ด้วยเสมอ (มีกฎรวมไว้ท้าย 4-components.css)

ระวัง selector ที่กว้างเกิน: `.side-brand span` เคยชนะ `.side-mark` ทำให้ `display:grid` ไม่ทำงาน
ไอคอนเลยไปกองมุมซ้ายบน และถูก `display:none` ที่จอ ≤1000px ไปด้วย — ให้เจาะจงเป็น `.side-brand > div span`

## หัวข้อกับค่าต้องสอดคล้องกัน

ทุกครั้งที่เขียน `label` ในการ์ดสรุป ให้เปิดหน้าข้อมูลดิบเช็กว่าคอลัมน์นั้นเก็บอะไรจริง ๆ
เคยเจอมาแล้ว: `label: "อาชีพ/กลุ่ม"` แต่ `column: Q.gender` (เพศ) และ `equals: "ดราม่า"`
ที่ไม่มีในตัวเลือกจริงเลย ทำให้ขึ้น 0% ทั้งที่จริง 30%

- ช่องที่เลือกได้หลายข้อ ต้องใส่ `multi: true` ตอน `agg:"distinct"`
  ไม่งั้นได้ "จำนวนรูปแบบการเลือก" ไม่ใช่ "จำนวนตัวเลือกที่มีคนใช้"
- `match` ใน ANALYSIS.causes ต้องตรงกับตัวเลือกจริงในฟอร์ม ตัวเลือกไหนไม่มีใน causes จะไม่มีทางแก้ขึ้นให้

## ตัวกรองคำตอบ: วิเคราะห์ก่อน แล้วค่อยคัด

ลำดับการคิดใน `core/quality.js`
1. ไม่มีเนื้อหา / อ่านไม่ออก -> คัดทิ้ง
2. **ลองจัดหมวดดูก่อน** ถ้าจัดหมวดได้ = มีความหมายต่อการวิเคราะห์ ให้ใช้เลย ต่อให้สั้นแค่ไหน
   ("พาดหัว" สั้นก็จริง แต่ตอบตรงคำถาม)
3. จัดหมวดไม่ได้ แต่ยาวพอ (14 ตัวอักษรขึ้นไป) -> เก็บไว้ ระบบจัดหมวดไม่ได้ไม่ใช่ความผิดผู้ตอบ
4. จัดหมวดไม่ได้ และสั้นด้วย -> คัดออก

ถ้าเจอคำตอบดี ๆ ถูกคัดออก ให้เพิ่มคำสำคัญใน `core/textAnalysis.js` ก่อน อย่าเพิ่งไปลดความเข้มของกฎ
