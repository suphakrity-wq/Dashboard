/* ===== ตั้งค่าที่ต้องแก้บ่อย =====
   เนื้อหาของแต่ละหน้าไม่ได้อยู่ที่นี่แล้ว — ดูที่ assets/js/pages/ (ไฟล์ละหน้า)  */

window.DASHBOARD_CONFIG = {
  /* ---- ชื่อที่แสดง ---- */
  brand: "News Behavior",
  brandSub: "ผลสำรวจพฤติกรรมเสพข่าว",
  title: "ทำไมคนไทยถึงชอบเสพสื่อ[ดราม่า] แต่ไม่เสพข่าวโลก?",   // [...] = คำที่เน้นสี

  /* ---- แหล่งข้อมูล ----
     ใช้ sheetId ของ Google Sheet ที่เปิดสิทธิ์ให้เข้าถึงแล้ว
     ถ้าต้องการชีตแท็บอื่น ใส่ gid: "ตัวเลขท้าย URL" เพิ่ม
     หรือจะใช้ csvUrl จาก File > Share > Publish to web ก็ได้ */
  sheetId: "14j4efyslFyV441M332hzPtnORddGdaD6MpnlrKLARS0",
  // gid: "0",
  // csvUrl: "",

  /* ---- ถ้ามีหลายแหล่ง (เช่น เก็บข้อมูลรอบสอง หรือแบบสอบถามชุดอื่น) ----
     ใส่เป็น sources แทน sheetId ด้านบนได้เลย ระบบจะรวมข้อมูลให้อัตโนมัติ
     และเพิ่มคอลัมน์ __source ไว้ให้กรองว่ามาจากแหล่งไหน

  sources: [
    { id: "round1", label: "เก็บรอบที่ 1", sheetId: "13meuLh…" },
    { id: "round2", label: "เก็บรอบที่ 2", sheetId: "xxxxxxx", gid: "0" }
  ],
  */

  /* ---- ดึงข้อมูลใหม่อัตโนมัติทุกกี่นาที (0 = ปิด) ---- */
  refreshMinutes: 3,

  /* ---- เก็บข้อมูลล่าสุดไว้ในเครื่องกี่นาที เพื่อให้เปิดครั้งถัดไปเห็นทันที ---- */
  cacheMinutes: 60
};
